import 'package:flutter/material.dart';
class AppColors {
  // class for the app colors
  static const Color DROTURQUOISE = Color(0XFF0CB8B6);

  static const Color DROPURPLE = Color(0XFF9F5DE2);

  static const Color DROBLUE = Color(0XFF5C86CE);

  static const Color DROPURPLELEFT = Color(0XFF7A08FA);

  static const Color DROPURPLERIGHT = Color(0XFFAD3BFC);

  static const Color DROREDLEFT = Color(0XFFFE806F);

  static const Color DROREDRIGHT = Color(0XFFE5366A);

  static const LinearGradient DROPURPLEGRA =
  LinearGradient(colors: [Color(0XFF7A08FA), Color(0XFFAD3BFC)]);

  static const LinearGradient DROREDGRA =
  LinearGradient(colors: [Color(0XFFFE806F), Color(0XFFE5366A)]);
}
